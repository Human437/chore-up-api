DROP TABLE IF EXISTS families;

CREATE TABLE families(
  id INTEGER PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY,
  admin NUMERIC NOT NULL,
  members INTEGER[] NOT NULL,
  code_2_join TEXT NOT NULL
);