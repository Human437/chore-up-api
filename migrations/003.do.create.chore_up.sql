DROP TABLE IF EXISTS families;

CREATE TABLE families(
  id INTEGER PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY,
  admin INTEGER REFERENCES users(id) ON DELETE CASCADE,
  code_to_join TEXT NOT NULL
);